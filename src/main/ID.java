package main;

public enum ID {

	Player(), //creates enumerations, so now we can ID something as
	Player2(), //a player or an enemy
	BasicEnemy(), 
	Boundaries(),
	Trail(),
	FastEnemy(),
	SmartEnemy(),
	EnemyBoss(),
	MenuParticle();
}
